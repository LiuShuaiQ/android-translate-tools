# Android Translate Tool

Organize strings.xml under Android resources into an excel table tool.

## How to build

build the package:
```shell script
./gradlew build
```

run the project:
```shell script
./gradlew run
```